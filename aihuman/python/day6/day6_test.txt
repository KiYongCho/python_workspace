AI휴먼 day6 단답형 테스트
주제: Python 함수(Function), 매개변수, 인수, return, Scope
총 18문항

[1] 특정 작업을 수행하도록 묶어 놓은 재사용 가능한 코드 블록을 무엇이라고 합니까?

[2] Python에서 함수를 정의할 때 사용하는 키워드를 쓰세요.

[3] 함수 정의 부분에서 입력값을 받을 자리에 작성하는 변수를 무엇이라고 합니까?

[4] 함수를 호출할 때 실제로 전달하는 값을 무엇이라고 합니까?

[5] 함수의 처리 결과를 호출한 위치로 돌려주는 키워드는 무엇입니까?

[6] 다음 코드의 출력값을 쓰세요.

def add(a, b):
    return a + b

print(add(3, 7))

[7] 다음 코드에서 매개변수의 이름을 모두 쓰세요.

def profile(name, job):
    print(name, job)

[8] 다음 호출에서 인수는 무엇입니까?

profile("민수", "개발자")

[9] 함수 내부에서 생성되어 일반적으로 함수 내부에서만 사용할 수 있는 변수를 무엇이라고 합니까?

[10] 함수 밖에서 생성되어 넓은 범위에서 참조할 수 있는 변수를 무엇이라고 합니까?

[11] 다음 코드의 출력값을 쓰세요.

def get_grade(score):
    if score >= 80:
        return "PASS"
    return "RETRY"

print(get_grade(75))

[12] return 문이 실행되면 현재 함수의 실행은 계속됩니까, 종료됩니까?

[13] 다음 코드에서 오류가 발생하는 이유를 간단히 쓰세요.

def add(a, b):
    return a + b

print(add(10))

[14] 다음 두 함수 중 결과값을 다른 계산에 다시 사용하기 더 적합한 함수를 A 또는 B로 쓰세요.

A.
def calc(a, b):
    print(a + b)

B.
def calc(a, b):
    return a + b

[15] 다음 함수 호출 방식의 이름을 쓰세요.

def introduce(name, job):
    print(name, job)

introduce(job="분석가", name="지수")

[16] 다음 코드에서 message의 기본값은 무엇입니까?

def greet(name, message="안녕하세요"):
    print(name, message)

[17] 프로그램을 평균 계산 함수, 등급 판정 함수, 결과 출력 함수처럼 여러 함수로 나누면 얻을 수 있는 장점 한 가지를 쓰세요.

[18] 다음 의사코드의 빈칸에 들어갈 가장 적절한 Python 키워드를 쓰세요.

함수 get_average(scores)
    total 계산
    average 계산
    ______ average
