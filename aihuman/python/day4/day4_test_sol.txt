AI휴먼 Day 4 단답형 테스트 풀이

1. 조건식(Condition)
   - True 또는 False로 평가되는 식입니다.

2. Boolean(bool)
   - Python에서는 True와 False 값을 사용합니다.

3. ==
   - =는 대입, ==는 비교입니다.

4. !=

5. and

6. or

7. 콜론(:)

8. else

9. elif

10. int()
    - input()의 결과는 문자열이므로 정수 비교를 위해 변환합니다.

11. PASS
    - 85 >= 80의 결과가 True입니다.

12. D
    - 95 >= 60이 첫 번째 조건에서 이미 True이므로 아래 elif는 검사하지 않습니다.
    - 이 예제는 조건의 순서가 중요하다는 것을 보여줍니다.

13. score >= 70 and attendance >= 80

14. score < 0 or score > 100

15. =를 ==로 수정합니다.
    정답 예:
    if score == 80:
        print("같음")

16. 높은 기준을 먼저 검사합니다.
    - 예: 90 이상 → 80 이상 → 70 이상 순서
